/* ===== Engine: Events ===== */
var engine;
(function (engine) {
    var Events = (function () {
        function Events() {
        }
        return Events;
    }());
    engine.Events = Events;
})(engine || (engine = {}));
